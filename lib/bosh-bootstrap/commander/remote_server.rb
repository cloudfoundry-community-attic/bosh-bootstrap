class Bosh::Bootstrap::Commander::RemoteServer
  # Execute the +Command+ objects, in sequential order
  # upon the target remote server
  # +commands+ is a +Commands+ container
  def run(commands)
    
  end
end