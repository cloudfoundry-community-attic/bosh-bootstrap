---
name: test-bosh
logging:
  level: DEBUG
cloud:
  properties:
    agent:
      ntp:
        - 0.foo.pool.ntp.org
        - 1.foo.pool.ntp.org
apply_spec:
  properties:
    ntp:
      - 0.foo.pool.ntp.org
      - 1.foo.pool.ntp.org

